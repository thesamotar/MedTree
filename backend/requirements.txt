# Pinned to the versions the app was built and tested against, so the Render build
# resolves the same dependency tree that works locally. cognee is the load-bearing pin.
fastapi==0.138.2
uvicorn==0.49.0
cognee==1.2.2
anthropic==0.113.0
pydantic==2.12.5
python-dotenv==1.0.1
google-genai==2.10.0
