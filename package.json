{
  "name": "medtree",
  "version": "1.0.0",
  "private": true,
  "description": "Medical Correlation Engine — Cognee Hackathon",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "seed": "cd backend && ./venv/bin/python seed.py",
    "backend": "cd backend && ./venv/bin/python main.py"
  }
}
